# 📌 COLA VISUAL - MAILJZTECH

```
╔══════════════════════════════════════════════════════════════════╗
║                  ARQUITETURA MVC - MAILJZTECH                   ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│                        📱 CLIENTE / API                          │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│  📄 CONTROLLER (src/controllers/)                                │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  ✅ FAZ:                    ❌ NÃO FAZ:                   │  │
│  │  • Recebe HTTP request      • Lógica de negócio            │  │
│  │  • Valida auth              • Acesso direto a Models       │  │
│  │  • Valida inputs            • Queries ao banco             │  │
│  │  • Chama Handler            • Envio de e-mails             │  │
│  │  • Retorna response         • Cálculos complexos           │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Exemplo:                                                         │
│  public function sendEmail() {                                    │
│      $dados = ctrl::getBody(true);                                │
│      $resultado = EmailsHandler::enviar(...);  ← Chama Handler   │
│      ctrl::response($resultado, 200);                             │
│  }                                                                │
└────────────────────────────┬──────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│  🔧 HANDLER (src/handlers/)                                       │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  ✅ FAZ:                    ❌ NÃO FAZ:                   │  │
│  │  • Validação de negócio     • Receber HTTP direto          │  │
│  │  • Orquestração             • Retornar response HTTP       │  │
│  │  • Criação de logs          • Queries SQL diretas          │  │
│  │  • Chama Services/Models    • -                             │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Exemplo:                                                         │
│  public static function enviar($idsistema, $idusuario, $dados) { │
│      Emails_logs::criar(..., 'validacao', 'Validando...');       │
│      $idemail = Emails_enviados::criar($dados);  ← Model         │
│      $resultado = EmailService::sendEmail(...);  ← Service       │
│      return $resultado;                                           │
│  }                                                                │
└────────────┬─────────────────────────┬─────────────────────────────┘
             │                         │
             ▼                         ▼
┌─────────────────────────┐  ┌─────────────────────────────────────┐
│ 📧 SERVICE              │  │ 💾 MODEL (src/models/)              │
│ (src/handlers/service/) │  │ ┌─────────────────────────────────┐ │
│ ┌─────────────────────┐ │  │ │  ✅ FAZ:      ❌ NÃO FAZ:      │ │
│ │ • SMTP (PHPMailer)  │ │  │ │  • CRUD       • Validação      │ │
│ │ • 2FA               │ │  │ │  • Queries    • Lógica negócio │ │
│ │ • APIs externas     │ │  │ │  • Acesso BD  • Enviar email   │ │
│ │ • Pode chamar Model │ │  │ └─────────────────────────────────┘ │
│ └─────────────────────┘ │  │                                     │
│                         │  │ Nome = Tabela (exato!)              │
│ EmailService::sendEmail │  │ emails_enviados → Emails_enviados  │
└─────────────────────────┘  └─────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════╗
║                    ENUM TIPOS DE LOG (6 VALORES)                 ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────┬──────────────────────────────────────────────────┐
│ Tipo ENUM    │ Quando Usar                                      │
├──────────────┼──────────────────────────────────────────────────┤
│ 'envio'      │ SMTP, conectando, enviando, enviado             │
│ 'criacao'    │ Criar registros no banco                         │
│ 'atualizacao'│ Atualizar registros                              │
│ 'erro'       │ Erros, exceções, falhas                          │
│ 'autenticacao'│ Login, logout, 2FA, tokens                      │
│ 'validacao'  │ Validar dados, testar configs                    │
└──────────────┴──────────────────────────────────────────────────┘

✅ CORRETO:
Emails_logs::criar($id, $sys, $usr, 'envio', 'Conectando SMTP...');
Emails_logs::criar($id, $sys, $usr, 'validacao', 'Testando config');
Emails_logs::criar($id, $sys, $usr, 'erro', 'Falha: ' . $e->getMessage());

❌ ERRADO:
Emails_logs::criar($id, $sys, $usr, 'smtp_conectando', '...'); // Não existe!
Emails_logs::criar($id, $sys, $usr, 'teste_iniciado', '...'); // Não existe!
Emails_logs::criar($id, $sys, $usr, 'fluxo_completo', '...'); // Não existe!


╔══════════════════════════════════════════════════════════════════╗
║                      TABELAS E MODELS                            ║
╚══════════════════════════════════════════════════════════════════╝

┌───────────────────┬─────────────────────┬────────────────────────┐
│ Tabela            │ Model               │ Import                 │
├───────────────────┼─────────────────────┼────────────────────────┤
│ emails_enviados   │ Emails_enviados.php │ use src\models\...     │
│ emails_logs       │ Emails_logs.php     │ use src\models\...     │
│ usuarios          │ Usuarios.php        │ use src\models\...     │
│ sistemas          │ Sistemas.php        │ use src\models\...     │
└───────────────────┴─────────────────────┴────────────────────────┘

REGRA: Nome do Model = Nome da Tabela (primeira letra maiúscula)


╔══════════════════════════════════════════════════════════════════╗
║                      IMPORTS CORRETOS                            ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ CONTROLLER                                                        │
├──────────────────────────────────────────────────────────────────┤
│ namespace src\controllers;                                        │
│                                                                   │
│ use core\Controller as ctrl;                                     │
│ use src\handlers\Emails as EmailsHandler;  ✅ Handler            │
│ use src\handlers\Logs as LogsHandler;      ✅ Handler            │
│ use src\models\Sistemas;  ✅ OK (validar se sistema existe)      │
│                                                                   │
│ ❌ use src\models\Emails_enviados;  ← NÃO! Use Handler!          │
│ ❌ use src\models\Emails_logs;      ← NÃO! Use Handler!          │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ HANDLER                                                           │
├──────────────────────────────────────────────────────────────────┤
│ namespace src\handlers;                                           │
│                                                                   │
│ use src\models\Emails_enviados;       ✅ OK                       │
│ use src\models\Emails_logs;           ✅ OK                       │
│ use src\handlers\service\EmailService;  ✅ OK                     │
└──────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════╗
║                    CHECKLIST ANTES DE CODAR                      ║
╚══════════════════════════════════════════════════════════════════╝

CONTROLLER:
  ☐ Importei apenas Handlers (não Models)?
  ☐ Validei auth/inputs?
  ☐ Chamei Handler (não Model)?
  ☐ Tratei exceções (try/catch)?

HANDLER:
  ☐ Criei logs nos pontos importantes?
  ☐ Usei tipos de log do ENUM?
  ☐ Validei regras de negócio?
  ☐ Chamei Models/Services corretamente?

MODEL:
  ☐ Nome é EXATO da tabela?
  ☐ Tem apenas CRUD (sem lógica)?
  ☐ Defini $table e $primaryKey?


╔══════════════════════════════════════════════════════════════════╗
║                     FLUXO DE UM REQUEST                          ║
╚══════════════════════════════════════════════════════════════════╝

1. POST /api/emails/send
   ↓
2. EmailController::sendEmail()
   ├─ Valida auth
   ├─ Valida campos (idsistema, destinatario, assunto)
   └─ Chama: EmailsHandler::enviar(...)
      ↓
3. EmailsHandler::enviar()
   ├─ LOG: 'validacao' → "Iniciando validação..."
   ├─ Valida negócio (destinatário, assunto, corpo)
   ├─ Cria registro: Emails_enviados::criar($dados)
   ├─ LOG: 'criacao' → "Registro criado"
   └─ Chama: EmailService::sendEmail(...)
      ↓
4. EmailService::sendEmail()
   ├─ LOG: 'envio' → "Conectando SMTP..."
   ├─ PHPMailer: configura + envia
   ├─ LOG: 'envio' → "E-mail enviado"
   └─ Atualiza: Emails_enviados::atualizarStatus($idemail, 'enviado')
      ↓
5. Retorna: ['sucesso' => true, 'idemail' => 123]


╔══════════════════════════════════════════════════════════════════╗
║                    ❌ ERROS COMUNS ❌                            ║
╚══════════════════════════════════════════════════════════════════╝

❌ EmailController chamando Emails_enviados::criar()
   ✅ EmailController chama EmailsHandler::enviar()

❌ Emails_logs::criar(..., 'smtp_conectando', ...)
   ✅ Emails_logs::criar(..., 'envio', 'Conectando SMTP...')

❌ class Emails extends Model // Nome diferente da tabela
   ✅ class Emails_enviados extends Model // Nome exato

❌ Handler com lógica de validação de auth
   ✅ Controller valida auth, Handler valida negócio

❌ Model com validação de e-mail
   ✅ Handler faz validação, Model faz CRUD


╔══════════════════════════════════════════════════════════════════╗
║                    ESTRUTURA DE PASTAS                           ║
╚══════════════════════════════════════════════════════════════════╝

src/
├── controllers/          📄 HTTP requests
│   ├── EmailController.php
│   └── LogsController.php
│
├── handlers/            🔧 Lógica de negócio
│   ├── Emails.php
│   ├── Logs.php
│   └── service/         📧 Serviços externos
│       ├── EmailService.php
│       └── TwoFactorAuthService.php
│
├── models/              💾 CRUD
│   ├── Emails_enviados.php
│   ├── Emails_logs.php
│   ├── Usuarios.php
│   └── Sistemas.php
│
└── views/               🖼️ Frontend
    ├── pages/
    └── partials/


╔══════════════════════════════════════════════════════════════════╗
║                       DOCUMENTAÇÃO                               ║
╚══════════════════════════════════════════════════════════════════╝

📚 docs/PADROES_PROJETO.md      - Guia completo (LEIA!)
📚 docs/GUIA_RAPIDO.md          - Referência rápida
📚 docs/COLA_VISUAL.md          - Este arquivo (imprima!)
📚 docs/CORRECAO_ARQUITETURA.md - Correções implementadas
📚 SQL/DDL_MAILJZTECH.sql       - Estrutura do banco


╔══════════════════════════════════════════════════════════════════╗
║               ✅ SEGUINDO ESTES PADRÕES:                         ║
║                                                                   ║
║  • Código limpo e organizado                                     ║
║  • Fácil manutenção                                              ║
║  • Escalável                                                     ║
║  • Logs completos                                                ║
║  • Arquitetura MVC correta                                       ║
╚══════════════════════════════════════════════════════════════════╝

Atualizado: 09/11/2025
```
